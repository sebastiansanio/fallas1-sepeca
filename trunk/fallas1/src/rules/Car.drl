package rules;
global java.util.List list

rule "Regla2"
    dialect "mvel"
    when
        a: Attribute (name == "Uso" && value == 3 )
        c: Car (frenos == 5)
    then
        c.selected = 1;
end